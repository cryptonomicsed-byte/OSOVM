# Deferred from build (2026-07-12)
- ase.move, tokenomics_invariants.move, ase_tests.move = RETIRED per codex §29 (Àṣẹ token killed → stablecoin model)
- elegbara_router.move, veilsim_integration.move = KEPT but use `ase`; need §29 stablecoin rewrite before rebuild
- *_tests.move = deferred (not needed to deploy)
