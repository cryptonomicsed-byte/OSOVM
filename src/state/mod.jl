# state/mod.jl — Ọ̀ṢỌ́ L1 State Module Entry Point
# Includes both Phase 15.2 (AgentState) and Phase 15.3 (WorkObject).
# Add `include("state/mod.jl")` to oso_vm.jl or any L1 entry point.

include("agent_state.jl")
include("work_object.jl")
